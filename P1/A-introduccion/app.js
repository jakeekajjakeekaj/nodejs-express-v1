// Para este caso vamos a realizar una simple función **OJO: SE CAMBIARÁ EL ESPACIO DE IDENTACIÓN A 2 PORQUE ES EL ESTÁNDAR QUE SE SUELE USAR EN JS**

function saludar(nombre) {
  return `Hola, ${nombre}`;
}

console.log(saludar("Jakeekaj"));

// Para ejecutar este código usando NODE, lo que se tiene que hacer es desde lo visual con ayuda de VSC como ya se sabe, o desde la propia terminal de aquí de VSC, accedemos a la ruta y escribimos "node app.js", lo cual sería usando la palabra reservada node y posterior el nombre del archivo que queremos ejecutar.