// Los módulos principales de Node.js, serían los módulos built-in, también llamados módulos core; básicamente son los módulos principales que vienen instalados con node.
// Algunos módulos built-in son:
    /*
        http
        https
        fs(file system)
        os(operating system)
        path
        y muchos más...
    */

// MÓDULO CONSOLE
// Es un módulo built-in que implementa funcionalidad similar a la consola de un navegador web, un ejemplo es el console.log, ese pertenece al módulo CONSOLE. En pocas palabras permite ejecutar el código y mostrarlo en la terminal en vez de necesitar si o si a la web.

// Algunos métodos útiles del módulo CONSOLE son:
    /*
        - console.log();
        - console.warn();
        - console.error();
        - console.assert();
        - console.table();
    */